workflow_id = "hello-world"
schema_version = 1

[[nodes]]
id = "start"
name = "Start"
outputs = ["stdout", "stderr", "exit_code", "result"]
timeout_sec = 30

[nodes.config]
program = "/bin/echo"
arguments = ["hello from DAGForge"]
