# Simple Example - Quick Test
# Use this to verify TaskMaster is working
#
scheduler:
  log_level: info

tasks:
  - id: echo
    name: Echo Test
    command: echo "TaskMaster is running! Time: $(date)"
    cron: "* * * * *"  # Every minute

  - id: echo2
    name: Echo Test 2
    command: echo "hello2"
    deps: [echo]
