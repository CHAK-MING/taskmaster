# add-http-executor

Add an executor-neutral asynchronous HTTP task executor with strict configuration, bounded responses, cancellation, admission-compatible registration, and real mixed-DAG end-to-end validation.
