# Tasks

- [x] Add focused external configuration headers under `dagforge/config`.
- [x] Preserve legacy TOML parsing while converting to the new config model.
- [x] Move Command and HTTP executor interfaces and implementations into
      executor-specific directories.
- [x] Remove single-user private headers and relocate necessary shared private
      headers under clear subsystem `detail` directories.
- [x] Move Minijail policy, launch, and process-management implementation
      declarations out of public include directories.
- [x] Update Application wiring, modules, build files, tests, and documentation.
- [x] Add the mandatory placement rules to `AGENTS.md` and coding guidance.
- [x] Run unit, real Workflow, sanitizer, module, convention, OpenSpec, and diff
      checks.
